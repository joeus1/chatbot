<!-- Injected into every session by .claude/hooks/session-prompt.sh; see that file. -->

HalalWay Toolkit active. Run `/verify` before every commit.

Commands: `/plan` `/review` `/security` `/tdd` `/fix` `/ship` `/cleanup` `/learn` `/verify`
